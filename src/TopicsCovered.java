public class TopicsCovered {
    // Reverse linked list
    // Binary search tree
    // Window sliding problem, substring without repeated chars
    // palindrome
    // permutations

    //longestpalindrome substring
    //min moves to sort
    //sorted array with repeated elements, get first index, last index of repeated element
    //non repeating shortest substring in array of words
    // inset, remove, getrandom in o(1) time
    //neighbors
    // word build

    /**
     * Types of for loops
     *      //Bubble sort
     *      for(int i=0;i<arry.length-1;i++){
     *          for(int j=0;j<array.length-1-i;j++)
     *              if(arry[j]>arry[j+1])
     *      }
     *      //Two sum, 3 sum, Longest palindrome
     *      for(int i=0;i<arry.lenght;i++){
     *          for(int j=i+1;j<arry.length;j++)
     *              if(arry[i]+arry[j]=target)
     *      }
     *      //Neighbors
     *      for(int i=0;i<arry.length;i++){
     *          for(int j=0;j<arry.length;j++){
     *              if(i=j) continue;
     *      }
     *      //Permutations
     *      //int pos = 0;
     *      for(int j=pos;j<arry.length;j++){
     *          permutation(pos+1, arry);
     *      }
     *      //Dupes from sorted array or uniques in sorted array
     *      int i=0;
     *      for(int j=1;j<arry.length;j++){
     *          if(arry[i]!=arry[j]){
     *              i++;
     *              arry[i]=arry[j];
     *          }
     *      }
     *      //Palindrome, reverse integer
     *      for(int i=0;i<arry.lenght/2;i++){}
     *
     */
}
